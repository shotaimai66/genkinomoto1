===============================
スタッフ様
===============================

<%= @user.name %> 様から下記内容で予約申込みがありました。
-----------------------------------------------------
【予約日】：<%= l @reservation.start_time, format: :dayonly %>
【時間】：<%= l @reservation.start_time, format: :timeonly %> - <%= l (@reservation.start_time + (@reservation.treatment_time_menu * 60)), format: :timeonly if @reservation.end_time.present? %>
【コース】：<%= @reservation.treatment_menu %>
【コメント】：<%= @reservation.comment.presence || "未記入"  %>
-----------------------------------------------------
